import type { FactsGameProps } from "@/app/utils/interfaces";

export default function SmallDisplay ({
    howPrize, resetGame, currentFact, pullRandomFact, pulledFacts, totalFacts, gameComplete
}: FactsGameProps) {

    return(
            <div id="smart-phone" className="block md:hidden">

            </div>
    )
}